#include<stdio.h>
#include<conio.h>
int main()
{
	int i,fact=1,n;
	printf("enter the N value");
	scanf("%d",&n);
	if(n>0)
	{
	for(i=1;i<=n;i++)
	{
		fact=fact*i;
	}
	printf("factorial of number %d = %d",n,fact);
}
  else
  {
  	printf("enter correct number");
  }
}
